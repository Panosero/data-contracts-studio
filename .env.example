# Data Contracts Studio - Environment Configuration
# Copy this file to .env and customize for your deployment

# =============================================================================
# 🌐 SERVER CONFIGURATION
# =============================================================================

# Server settings (change these for your deployment)
SERVER_IP=auto-detect
FRONTEND_PORT=80
BACKEND_PORT=8888
DATABASE_PORT=5432

# =============================================================================
# 🔒 SECRETS (IMPORTANT: Keep these secure!)
# =============================================================================

# Database credentials
POSTGRES_DB=datacontracts
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-this-secure-password

# Application security
SECRET_KEY=change-this-to-a-secure-random-string

# =============================================================================
# 🤖 API KEYS (Add your production keys here)
# =============================================================================

# OpenAI API Key (for LLM features)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Other API keys you might need
# ANTHROPIC_API_KEY=your-anthropic-key
# HUGGINGFACE_API_KEY=your-huggingface-key
# AZURE_OPENAI_KEY=your-azure-key

# =============================================================================
# 🎛️ APPLICATION SETTINGS
# =============================================================================

# Application configuration
DEBUG=False
ENVIRONMENT=production
APP_NAME=Data Contracts Studio
APP_VERSION=1.0.0

# CORS settings (automatically configured based on SERVER_IP)
ALLOWED_ORIGINS=["http://localhost"]

# Database URL (automatically constructed)
DATABASE_URL=postgresql://postgres:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}

# Frontend API URL (automatically configured)
REACT_APP_API_URL=http://${SERVER_IP}:${BACKEND_PORT}/api/v1

# =============================================================================
# 📝 QUICK CONFIGURATION EXAMPLES
# =============================================================================

# EXAMPLE 1: Standard server deployment
# SERVER_IP=192.168.1.100
# FRONTEND_PORT=80
# BACKEND_PORT=8888

# EXAMPLE 2: Shared server (avoid port conflicts)
# SERVER_IP=192.168.1.100
# FRONTEND_PORT=8080
# BACKEND_PORT=8001
# DATABASE_PORT=5433

# EXAMPLE 3: Custom domain
# SERVER_IP=data-contracts.company.com
# FRONTEND_PORT=80
# BACKEND_PORT=8888
